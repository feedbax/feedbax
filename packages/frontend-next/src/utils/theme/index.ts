import { colors } from '@/utils/theme/colors';
import { fonts } from '@/utils/theme/fonts';

export const themeStyles = {
  ':root': {
    ...colors,
    ...fonts,
  },
};

export { colors } from '@/utils/theme/colors';
export { fonts } from '@/utils/theme/fonts';
